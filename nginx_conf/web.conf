server {
    listen       80;
    location / {
      proxy_set_header        Host $host;
      proxy_pass      http://sr-provider:8002;
    }
    location /media {
        autoindex on;
        alias /usr/src/app/media/;
    }

 }
